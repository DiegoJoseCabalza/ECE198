#store test functions

#scratch test objective function
def obj_func(x,y):
    f = 0
    for i in range(y):
            f += x[i][:]
    return f

#velocity computation, closer look on scikit functions
def _compute_velocity(a,c1,c2,w):
    for i in range(particles):                  
        for j in range(dimensions):
            c1*np.random.uniform(low=0.0, high=1.0)
            c2*np.random.uniform(low=0.0, high=1.0)
    return 

#compute position, kulang pa
def _compute_position(a)

    for i in range(x)
        for j in range(y)
            np.random.uniform(low=0.0, high=1.0) ##to fix, kulang pa
            if
            a = 1
            else
            a = 0
    return a

